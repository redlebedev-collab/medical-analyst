# Медицинский аналитик — зависимости
# Установка: pip install -r requirements.txt

# Интерфейс
streamlit>=1.35.0        # Веб-приложение с drag & drop

# LLM-бэкенды
anthropic>=0.40.0        # Claude API (облачный режим)
ollama>=0.3.0            # Ollama (локальный режим)

# Работа с документами
pdfplumber>=0.11.0       # Извлечение текста из PDF
PyYAML>=6.0              # Работа с YAML
python-docx>=1.1.0       # Генерация DOCX
pandas>=2.0.0            # Таблицы и графики в Streamlit

# OCR (опционально — для сканированных документов)
# Требует установки Tesseract: https://github.com/tesseract-ocr/tesseract
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.17.0        # Конвертация PDF → изображения для OCR
# Для pdf2image нужен Poppler: https://github.com/oschwartz10612/poppler-windows

# Утилиты
python-dotenv>=1.0.0     # Загрузка .env файла
